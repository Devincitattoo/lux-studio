{
  "name": "reply-assistant",
  "version": "0.2.0",
  "description": "Claude-powered SMS auto-reply on Twilio Serverless (Twilio Functions), with a review queue for anything that isn't routine.",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
